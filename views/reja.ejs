<!DOCTYPE html>
<html lang="uz">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>📋 REJA - Maqsadlar Boshqaruvi</title>
  <meta name="description" content="Reja va Maqsadlar — Your personal daily planner." />
  <meta name="author" content="Utkirbek Mekhmonboev" />

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Custom CSS -->
  <style>
    body {
      background-color: #f4f6f9;
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
    }

    .main-wrapper {
      max-width: 720px;
      margin: auto;
      padding: 2rem 1rem;
    }

    .card {
      border-radius: 1rem;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.06);
    }

    .btn-rounded {
      border-radius: 2rem;
    }

    .item-text {
      font-weight: 500;
      word-break: break-word;
    }

    .list-group-item {
      border-radius: 0.5rem;
      margin-bottom: 0.5rem;
    }

    footer {
      margin-top: 4rem;
      font-size: 0.9rem;
      color: #888;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="main-wrapper">
    <h1 class="text-center display-5 mb-4 text-primary">📋 REJA VA MAQSADLAR</h1>
    <p class="text-muted text-center mb-4" style="font-size: 0.9rem;">
  ⏳ Dastur 15 daqiqadan so‘ng uxlab qoladi. Ochishda biroz kuting...
</p>

    <!-- Form -->
    <div class="card p-4 mb-4 bg-white">
      <form id="create-form" action="/create-item" method="POST">
        <div class="row g-3">
          <div class="col-12 col-md-9">
            <input
              id="create-field"
              name="reja"
              type="text"
              class="form-control"
              placeholder="Bugungi reja..."
              autocomplete="off"
              required
            />
          </div>
          <div class="col-12 col-md-3 d-grid">
            <button class="btn btn-primary btn-rounded" type="submit">
              ➕ Qo‘shish
            </button>
          </div>
        </div>
      </form>
    </div>

    <!-- Item List -->
    <ul id="item-list" class="list-group mb-4">
      <% items.map(function(item){ %>
        <li class="list-group-item d-flex justify-content-between align-items-center bg-light">
          <span class="item-text"><%= item.reja %></span>
          <div class="d-flex gap-2">
            <button
              data-id="<%= item._id %>"
              class="edit-me btn btn-sm btn-outline-secondary"
            >
              ✏️
            </button>
            <button
              data-id="<%= item._id %>"
              class="delete-me btn btn-sm btn-outline-danger"
            >
              🗑️
            </button>
          </div>
        </li>
      <% }) %>
    </ul>

    <!-- Delete All Button -->
    <div class="d-grid">
      <button id="clean-all" class="btn btn-danger btn-rounded">
        🧹 Hamma rejalarni o‘chirish
      </button>
    </div>

    <!-- Footer -->
    <footer class="mt-5">
      &copy; <%= new Date().getFullYear() %> REJA App by Utkirbek Mekhmonboev
    </footer>
  </div>

  <!-- Scripts -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/axios/1.9.0/axios.min.js"></script>
  <script src="/browser.js"></script>
</body>
</html>
